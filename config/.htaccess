# Dieses Verzeichnis ist privat. Greift nur, wenn es (bei FTP-Hosting) innerhalb des Webroots liegt.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
